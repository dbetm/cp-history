#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]) {
    double x, y, res;
    cin >> x >> y;
    res = sqrt(pow(x, 2)+pow(y, 2));
    cout << res << endl;
    return 0;
}
